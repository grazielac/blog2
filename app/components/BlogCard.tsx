import React from 'react'

interface BlogCardProps {
    title: string;
    img: url;
    description: string;
    author: string;
    date: string;
}

function BlogCard({ title, img, descrciption, author, date }: BlogCardProps) {
  return (
    <div className="bg-white p-">
      
    </div>
  )
}

export default BlogCard
